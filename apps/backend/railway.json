{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx --prefix apps/backend prisma generate && npx --prefix apps/backend nest build"
  },
  "deploy": {
    "startCommand": "sh -c 'cd apps/backend && npx prisma migrate deploy && node dist/main'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
