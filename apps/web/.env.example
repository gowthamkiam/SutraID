# Frontend Environment Template (apps/web)
# ----------------------------------------
# Copy this file to `.env.local` (which is gitignored)
# and replace values as needed.

# Local development (backend running on host)
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"

# Example production configuration (replace with your own backend URL)
# NEXT_PUBLIC_API_URL="https://your-backend.example.com/api/v1"

